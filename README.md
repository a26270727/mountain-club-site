# 登山社團活動許願與報名網站

這是一個可直接開啟的靜態網站，不需要安裝後端或資料庫。

## 使用方式

1. 用瀏覽器開啟 `index.html`。
2. 社員可在「發想人許願」新增想去的山。
3. 社員可在「報名時間登記」新增姓名與想去日期。
4. 管理者在「補助款管理」輸入密碼 `2738` 後，可更新剩餘補助款。

## 資料儲存

資料會保存在目前瀏覽器的 `localStorage`。同一台電腦、同一個瀏覽器會保留資料；換電腦或換瀏覽器不會同步。

如果未來要多人共同使用，需要再接資料庫或 Google Sheet / Firebase 之類的後端服務。
